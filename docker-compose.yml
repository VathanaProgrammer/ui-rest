version: '3.8'
services:
  ui:
    build: .
    container_name: ui-rest
    ports:
      - "6555:80"
    restart: always

  caddy:
    image: caddy:2
    container_name: ui-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:
